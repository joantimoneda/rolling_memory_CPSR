# Copy this file to ".env" and add your own key, OR export the variable in your shell:
#   export OPENAI_API_KEY="sk-..."
# Only required to RE-RUN the LLM annotations (the generation scripts).
# Reproducing the figures from the bundled results does NOT need a key.
OPENAI_API_KEY=
